package io.scalechain.blockchain.script

trait Arithmetic extends ScriptOp

case class Op1Add() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class Op1Sub() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class Op1Mul() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class Op2Div() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}


case class OpNegate() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpAbs() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpNot() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class Op0NotEqual() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpAdd() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpSub() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpMul() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpDiv() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpMod() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpLShift() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpRShift() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpBoolAnd() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpBoolOr() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpNumEqual() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpNumEqualVerify() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}


case class OpLessThan() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}


case class OpGreaterThan() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}


case class OpLessThanOrEqual() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}


case class OpGreaterThanOrEqual() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpMin() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpMax() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}

case class OpWithin() extends Arithmetic {
  def execute(stack : ScriptStack): Unit = {
    assert(false);
    // TODO : Implement
  }
}
