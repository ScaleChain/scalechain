package io.scalechain.blockchain.script

import org.scalatest._

/** Test flow control operations in FlowControl.scala
  *
  */
class FlowControlSpec extends FlatSpec with BeforeAndAfterEach with ShouldMatchers {

  this: Suite =>

  override def beforeEach() {
    // set-up code
    //

    super.beforeEach()
  }

  override def afterEach() {
    super.afterEach()
    // tear-down code
    //
  }

  "method" should "do something" in {
  }
}
